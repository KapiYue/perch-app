# 更新日志

Perch（栖架）已发布和**已经决定要做**的功能都记在这里。
还在权衡、没想清楚的想法不写进来 —— 写进来就等于对外承诺。

版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)，日期为 `YYYY-MM-DD`。
这份清单只讲**做什么**。开发顺序与验收状态是维护者的内部材料，不在公开仓库里。

---

## [未发布] 1.0.0 · 本体

一句话：**拖进来、复制过的文件、图片、文本、链接先「栖」在屏幕顶部，随时再取到任何地方去。**

1.0 的范围就是把这件事做透，不做第二件事。

### 入口 · 屏幕顶部热区

- 入口定义是「屏幕中上方的一块热区」，不是「物理刘海」。有刘海的机器贴着刘海渲染；
  无刘海的机器（外接屏 / iMac / Mac mini）在顶部正中渲染一条同尺寸的黑色圆角条，两种情况面板外观完全一致
- 三种唤出：单击热区 / 悬停展开（鼠标移开 0.4 秒自动收起）/ 全局快捷键 `⌃⌘V`
- 拖着文件悬停到热区上自动展开并接收放下，拖拽期间面板不会自己溜走
- **每块屏幕各有一个热区**，面板落在触发它的那块屏上；接屏 / 断屏自动重建
- 黑条横向形变展开到 640pt 与面板连成一体，尊重系统的「减弱动态效果」
- 面板展开时**前台 App 的输入光标不会消失**，可以一边打字一边取内容

### 剪贴板 · 复制即上架

- 后台每 0.5 秒检查系统剪贴板变更计数，变了才读内容
- 类型按优先级判定：图片 → 文件 URL → 链接 → 纯文本
- 文本 / 链接 / 图片自动分类并带类型色标，图片显示真实缩略图（不是拿原图缩放）
- 前九条带 `⌘1`–`⌘9` 序号
- 最多保留 200 条，超出淘汰最旧的；固定项不参与淘汰
- 复制的文件路径自动转入文件区，不占剪贴板的位置
- 面板头部常驻 **⏸ 暂停按钮**，状态跨重启保留

### 剪贴板 · 类型筛选与代码识别 🆕

2026-08-21 追加进 1.0 范围。

- **类型筛选 pills**：全部 / 文本 / 链接 / 图片 / 代码，单选切换，切换不改动数据只改显示
- **新增 `code` 类型**：从剪贴板抓到的代码片段单独成一类，识别出语言后一并记录并显示在行内
  （`swift` / `python` / `json` …）。识别不出语言就退回普通文本，不硬猜
- 代码类型有自己的色标，与文本 / 链接 / 图片 / 文件并列

### 四条取出路径

| 操作 | 结果 |
|---|---|
| 单击整行 | 内容复制回剪贴板，行内绿色反馈 1.1 秒后连同面板一起收起 |
| `⌘1`–`⌘9` | 取第 N 条，手不离键盘（**只在面板展开时生效**，收起后立刻还给其它 App） |
| 拖住往外拖 | 落地成真文件（文字 → `.txt`，截图 → `.png`） |
| 双击整行 | 切换固定状态，固定项永不过期 |

### 文件 · 临时中转架

- 从访达拖进来，切 Space、切 App，再拖到目标位置
- **拖进来的文件会被拷贝一份**，之后移动 / 重命名 / 删掉原文件，架上这份都还在
- `⌘` / `Shift` 多选，`⌘A` 全选、`Esc` 取消选中，选中后批量：导出 / 打包 ZIP / 固定 / 移除
- **「导出」和「打包 ZIP」既能点也能拖**：点一下存到「下载」文件夹并在访达里选中它，
  直接拖走则落点由你定。打包成 zip 这件事发生在**松手那一刻**，拖到一半放弃不会留下任何东西
- 打包出来的 zip 解开是一个文件夹，重名文件自动错开，不会少东西
- 空格键 Quick Look 预览，双击用默认 App 打开
- 右键：打开 / 在访达中显示 / 复制路径 / 重命名 / 导出到「下载」/ 打包 ZIP / AirDrop 分享 / 固定 / 移除

### 持久化与自动清理

- 索引原子写入（先写 `.tmp` 再 rename），图片和文件只存路径，**绝不 base64 进 JSON**
- 保存时长可选 1 小时 / 12 小时 / 24 小时 / 7 天 / 永不，默认 **12 小时**
- 这是**唯一**的自动清理依据，没有其他隐藏触发条件；固定条目永不过期
- 重复复制同一内容不新增条目，把原来那条**提到最前**并重置倒计时
- 剪贴板最多 200 条，超出淘汰最旧的；**固定项先无条件保住**，不参与淘汰
- 贴着上限时面板上明确标出「已满」—— 淘汰按条数、清理按时长是两条独立规则，
  只显示倒计时会让人以为最旧那条还能活满，实际上它下一次复制就会被顶掉
- 清理条目时同步删除本体目录，不留孤儿文件
- 取消固定时，保存时长从那一刻重新起算（不是从当初复制那一刻）

### 设置

- 保存时长（1 小时 / 12 小时 / 24 小时 / 7 天 / 永不）
- 悬停时自动展开面板，可关闭；关掉之后仍可单击黑条、`⌃⌘V`、菜单栏图标唤出，
  拖着文件停到黑条上也照样展开
- 跳过来源 App 声明「不要记录」的内容（`org.nspasteboard.ConcealedType` / `TransientType` /
  `AutoGeneratedType` 等社区约定标记），**默认关闭** —— 可靠性取决于来源 App 规不规范打标
- **忽略指定 App 复制的内容**，**默认开启**，已预置「密码」与「钥匙串访问」，可自行增删。
  这条只看内容由哪个 App 提供，**不读取内容本身**
- **抹掉全部数据** —— 剪贴板历史、文件区、磁盘上的本体一起删掉

### 隐私

- 数据全部存在 `~/Library/Application Support/Perch/`，**一个字节都不上传**
- 没有账号、没有云同步、没有遥测、没有崩溃上报
- 运行期间不发起任何网络请求（Sparkle 更新检查除外，可关闭）
- 不申请任何需要用户点「允许」的系统权限：不要辅助功能、不要屏幕录制、不装全局鼠标钩子
- 可选：跳过被来源 App 标记为「不要记录」的内容（`org.nspasteboard.ConcealedType` 等），**默认关闭**
- ⚠️ **macOS 自带的「密码」App 复制密码时不打任何标记**（2026-08-25 实测），
  在剪贴板上和普通文字没有区别。它由「忽略指定 App」那条规则覆盖，默认已经在名单里

### 变更

- **许可证 MIT → GPL-3.0**（2026-08-21）。目的只有一条：别人可以拿去用、拿去改，
  但改完拿去卖就必须把源码一起放出来。对普通用户没有任何影响。
  1.0.0 之前公开过的版本仍以 MIT 授权流通，撤不回来也不打算撤

### 明确不做（1.0 及之后都不做）

- **不做抖动召唤手势** —— 那个交互解决的是「手上正拖着东西、临时变出货架」，
  Perch 的用法是「先攒着，过一会儿再取」。砍掉它省去了全局鼠标钩子
- **不做密码 / 敏感内容的启发式识别** —— 基于猜测的「智能过滤」一定会漏，
  而漏掉的那次恰恰最危险。配套补偿是常驻的暂停按钮和「抹掉全部数据」。
  两条隐私规则都**不违反**这一条：它们看的是「来源 App 有没有主动声明」和「内容是谁给的」，
  从不读取、也从不猜测内容本身
- **不做文件标签、分类、搜索** —— 那是文件管理器的活
- **不做自动化脚本引擎** —— 会把「抬手就用」变成「先配置」
- 剪贴板用**文本列表而非卡片**、复制反馈**只做行内**、**上下分区**而非单一列表 ——
  都是返工一轮后确认的结论

---

## [计划] 1.1.0 · 端到端加密云备份

**下一期做，1.0 发布并自用两周之后才开工。**

云备份是 1.0 那条「一个字节都不上传」的例外，所以它的每一条都是**默认关闭、用户显式打开**才生效。

### 端到端加密

- 加密和解密**只在本机发生**。上传的是密文，服务端拿到的也只有密文
- 密钥由用户口令派生，**不上传、不托管、不做找回**。口令丢了 = 备份解不开，
  这一点会在开启备份时用一屏说清楚，而不是藏在条款里
- 服务端不持有明文，也就不需要用户信任服务端不看

### 登录改成可选，不强制

- **不登录也能用全部本地功能。** 1.0 的所有能力在 1.1 里一条都不需要账号
- 账号只是「云备份」这一个功能的开关。不开云备份，就永远不会出现登录界面
- 已登录状态可以随时退出，退出后本地数据不受影响

### 本地不识别，上云才防护

延续 1.0 的口径：**本地不做敏感内容识别**（会漏，且漏的那次最危险）。
但内容一旦要离开这台机器，风险模型就变了，所以：

- 开启云备份时明确提示「架上的内容包括你复制过的密码和验证码」
- 提供「不备份哪些类型」的选择（例如只备份文件、不备份剪贴板文本）
- 被来源 App 标记为机密的内容**永不上云**，与本地那个开关无关

### 合规

- 持有账户即进入《个人信息保护法》《网络安全法》的义务范围：
  独立的隐私政策、账号注销通道、访问日志、泄露上报流程，上线前全部落地
- 官网隐私页、首页隐私区、两份 README 里「没有账号系统 / 不需要注册」的表述
  必须在这一版同步改写，不留自相矛盾

---

## [计划] 1.2.0 · 自动化外接口

- Shortcuts 动作、URL scheme、命令行入口，让别人用自己的工具去驱动 Perch
- 一个只读的状态文件，供外部程序读取当前架上的内容
- **仍然不做插件宿主** —— 别人的代码不进 Perch 的进程，崩溃、性能、安全就都不是我们的问题

---

## 已发布

还没有。1.0.0 的进度会在这份更新日志和 GitHub Releases 上同步。
