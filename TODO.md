# Perch 执行顺序

更新日期：2026-08-15

**读法**：阶段之间是**依赖顺序**，阶段内部大致按先后排。标 🔴 的是阻塞项，不做完后面会返工。标 ⚡ 的可以和别的阶段并行。

配套文档：[发布清单](docs/release-checklist.md)（项目状态 + 市场定位 + 发版检查项）、维护者本地的备案手册 / 部署手册 / 腾讯云配置记录 / 签名与更新手册。

---

## 阶段 0 · 现在就做

- [x] ~~删除并重建 COS 桶~~ → **2026-08-14 15:38 完成**，桶名 `perch-dist-1305706023`
- [x] ~~验证公有读~~ → curl 返回 **404 NoSuchKey**（不是 403 AccessDenied），说明匿名读已生效、只是 DMG 还没上传。M6 有真包后再复验一次 200
- [x] ~~确认备案口径~~ → **2026-08-14 15:54 腾讯云工程师答复：子域名不需单独备案；macOS 桌面软件不属于移动互联网应用程序，也不需要备案。** 对话截图已存档，备案侧零待办
- [x] ~~`git init`~~ → 已完成，`.gitignore` 实测挡住了内部材料
- [x] ~~COS 生命周期加规则~~ → **2026-08-14 已配置**：`abort-multipart-7d` / 整个存储桶 / 碎片删除 7 天 / 开启，且没有「当前版本文件删除」
- [ ] 首次 commit 并推到 GitHub 仓库 `KapiYue/perch-app`
- [x] ~~确认 Apple Developer 会员到期日~~ → **有效至 2027-07-16**，Team ID `HJ7HXSM7Y9`，个人账号
- [ ] ⚡ **新建 `Developer ID Application` 证书**（M6 前完成即可，**不阻塞 M0–M5**）。
      已核实本机只有 `Apple Development` ×2 与 `Apple Distribution` ×1 —— 那两类都无法用于商店外分发和公证。步骤见签名手册第 1.4 节

---

## 阶段 1 · 官网上线 ⚡（可与阶段 2 完全并行）

站点五个页面已完成，备案不用新办，**不依赖 App 有没有做好**。

- [ ] 🔴 **先决定下载按钮怎么办。** DMG 要等 M6 才有，现在上线的话三处下载按钮会 404。两个选择：
      **(A) 推荐** —— 把下载区临时改成「即将发布」状态，引导去 GitHub 点 star；等 M6 换回真实链接。
      **(B)** 等 M6 完成后再一起上线，但这样 `/privacy`、`/support` 长期不可访问。
      选 A 的话这一步就是改 `site/index.html` 的下载区文案。
- [ ] DNS 加 A 记录：`perch` → `58.87.65.155`，`dig +short perch.joy-coder.com` 输出必须等于该 IP。
- [ ] 申请证书并安装到 `/etc/nginx/ssl/perch/`。
- [ ] `rsync` 上传 `site/`（排除 `serve.py`、`.DS_Store`），启用 `deploy/nginx/perch.conf`，`nginx -t` 通过后 reload。
- [ ] 公网验收：`/`、`/features`、`/privacy`、`/support`、`/credits`、两个 assets 全部 200 且 `verify=0`。
- [ ] 五个页面页脚核对：`浙ICP备2026055717号-2` + `浙公网安备33010502013311号` + 图标不裂，**没有误挂词鲸的 `-3A`**。
- [ ] ⚡ 补 `site/favicon.svg`（当前是内联 data URI，能用但换成真文件更规范）。

---

## 阶段 2 · App 开发 M0–M6

**一次只跑一段提示词，跑完手动验收再进下一段。** macOS 拖拽的问题编译期看不出来，只有真机拖一次才知道。

### M0 · 工程骨架（约 0.5 天）
- [x] ~~XcodeGen `project.yml` + 全部占位文件按既定目录结构生成~~ → 20 个 Swift 文件 + 资源，`swiftc -typecheck -swift-version 6` 通过，0 错误 0 警告
- [x] ~~菜单栏常驻，无 Dock 图标~~ → `LSUIElement = true` 已写入 Info.plist
- [x] ~~沙盒关闭~~ → `Perch.entitlements` 里 `com.apple.security.app-sandbox = false`，已用 plistlib 核验
- [x] ~~i18n 骨架~~ → `Localizable.xcstrings` 10 个键，zh-Hans + en 均已填，无漏翻
- [x] ~~`script/build_unsigned.sh`~~ → 已写好并 `chmod +x`，`bash -n` 通过
- [x] ~~**验收**~~ → **2026-08-14 本机全部通过**：
      构建链路通（xcodegen + xcodebuild）／菜单栏图标与三项菜单正常／
      进程在跑（`pgrep -x Perch` 有 PID）同时 **Dock 无图标、⌘Tab 切不到**／
      面板弹出、中文文案正常（String Catalog 生效）／
      **面板弹出时前台 App 输入光标仍在原处闪烁** ← `.nonactivatingPanel` 已验证生效

> ✅ **M0 完成。** 期间修掉两个真 bug：UTF-8 locale 下 `$VAR` 紧跟全角字符被 bash 吞字节；
> 面板用 `screen.frame` 定位盖住菜单栏导致无法收起。两处都已写进代码注释。

### M1 · 面板 + 拖入 + 拖出 🔴 生死线

拆成两段跑：**先花半天只验证拖出**，通过了再补剩下的。
理由是拖出（`NSFilePromiseProvider`）是唯一「做不出来项目就废了」的能力，
先把热区、拖入、缩略图堆上去，万一拖出不通，那几天全白做。

#### M1-a · 只做拖出（约 0.5 天）

- [x] ~~`FilePromiseProvider` + `DragOutCoordinator`：两条路径分派~~ →
      blobs/ 里有本体的走 `.fileURL`，磁盘上还不存在的走承诺
- [x] ~~`DragSourceView`：AppKit 起拖层，SwiftUI 用 `.dragOut([item])` 挂上去~~ →
      SwiftUI 的 `.onDrag` 只能给 `NSItemProvider`，喂不出文件承诺，所以这层必须下沉到 AppKit
- [x] ~~自检脚手架 `DragOutSelfCheck`（DEBUG）~~ → 启动塞两条假数据，
      因为这一段还没有任何往架上放东西的入口。**M1-b 做完删掉整个文件**
- [x] ~~无鼠标可验的部分~~ → 承诺文件名清洗、UTF-8 落盘、pasteboard 类型、
      本体丢失时不给拖，四项已用命令行 probe 跑通
- [x] ~~**验收**~~ → **2026-08-15 通过**：④ 文本拖到桌面生成 `.txt`，内容正确 ← 生死线已解除

> 🚨 **第 ④ 条做不通就停下来，不要绕过去继续堆功能。** 后面全部功能都架在它上面。

**M1-a 期间记下来的三个坑**（都已写进代码注释）：
1. `NSFilePromiseProvider.delegate` 是 **weak** 的 —— delegate 被释放后拖出会*静默*失败，
   目标位置什么都没有且控制台不报错。所以 delegate 只能是 `DragOutCoordinator.shared`。
2. `operationQueueForFilePromiseProvider:` 是**可选**方法，Swift 名字写错也照样编译，
   只是永远不被调用、退回主队列写盘。已核对生成的 selector 确认挂上了。
3. 面板不抢焦点 ⇒ Perch 永远不是前台 App ⇒ 每一次点击都是「非活跃窗口的第一次点击」。
   `acceptsFirstMouse` 不返回 true 的话，第一下会被系统吞掉，表现为「要点两次才拖得动」。

#### M1-b · 面板 + 热区 + 拖入（约 2–3 天）

- [x] ~~`HotZoneWindow` 190×30pt~~ → 单击切换 / 悬停展开 / 移开 0.4s 收起 / 拖拽悬停展开并粘住
- [x] ~~`PanelController`：热区与面板的唯一调度者~~ → 区分「悬停展开（会自动收）」
      和「点击或拖拽展开（粘住）」两种来路。拖东西的时候面板绝对不能自己溜走
- [x] ~~面板改回贴屏幕**物理**顶部~~ → 前提已满足，见下方「收起入口」
- [x] ~~展开/收起 spring 动画~~ → 动画对象是**内容 offset**，不是窗口 frame：
      窗口尺寸每帧变的话里面的 SwiftUI 布局跟着每帧重算，掉帧很明显
- [x] ~~拖入：多文件全部接收，拷贝进 `blobs/`~~ → probe 实测 3 个文件全进、
      **删掉原文件后本体仍在**（存引用的话这里就断了）
- [x] ~~`QLThumbnailGenerator` 生成缩略图~~ → 异步生成，先上架后补图，失败静默回退系统图标
- [x] ~~删掉 `DragOutSelfCheck.swift` 与 `AppDelegate` 里的调用~~
**2026-08-15 第一轮真机反馈后补的三项**：

- [x] ~~🔴 **每块屏幕一个热区**~~ → 原来只建了一个挂在 `NSScreen.main` 上，
      另一块屏的顶部完全没有入口。现在按 `NSScreen.screens` 逐块建，
      并监听 `didChangeScreenParametersNotification` 在接屏/断屏时重建。
      展开时面板落在**触发它的那块屏**上（`HotZoneWindow.screen0`），不是永远落主屏。
      实测：内置屏热区在 `(661,0)`、外接屏在 `(2377,-202)`，两块都在物理顶部
- [x] ~~**取消「点击粘住」**~~ → 口径统一成「**没悬停就收起**」。
      点击只是立刻展开／立刻收起。原来点击展开会粘住，拖出去之后面板一直挂着挡路
- [x] ~~**拖出结束后收起面板**~~ → 🚨 拖拽期间系统**不发** `mouseExited`，
      `isHovering` 会一直停在 true。必须在 `draggingSession(_:endedAt:operation:)`
      里拿 `NSEvent.mouseLocation` 兜底重算，否则这条永远不会生效
- [x] ~~**文件区多选拖出**~~ → ⌘/Shift 点击累加，拖已选中的格子 → 整个选中集一起走
      （规则和访达一致）。拖完清空选中。批量操作条和 ZIP 仍在 M4；
      **⌘A / Esc 做不了** —— 面板 `canBecomeKey = false` 收不到键盘事件，
      要挂全局 `NSEvent` 监听，留给 M4

- [x] ~~**验收（必须真机拖一次）**~~ → **2026-08-15 全部通过**：
      ① 拖 3 个文件进去 → 切 Space → 拖进另一个 Finder 窗口，3 个都在
      ② 拖面板里的文件到浏览器上传框，能上传成功
      ⑤ **两块屏**顶部悬停都能展开，面板出现在当前那块屏上
      ⑥ ⌘ 点选 3 个文件一起拖出 → 3 个都落地，**拖完面板自动收起**
      ~~③ 面板展开时前台 App 输入光标不消失~~ → **M0 已提前验证通过**

> ✅ **M1 完成，生死线整体解除。**

> ⚠️ **黑条和面板一起盖住菜单栏中段了。** 收起入口有三个，都在 640pt 覆盖范围之外：
> ① 黑条单击（黑条 `order(.above:)` 压在面板上）；② 菜单栏图标（在最右侧）；③ 菜单里的「打开面板」。
> **改动面板宽度、位置或黑条层级前，先确认这三个入口还在** —— M0 那次就是把唯一入口盖死，只能 `pkill`。

#### M1-c · 黑条常驻 + 形变展开（从 M5 提前，约半天）

外观定稿的第一步，只动 `HotZoneWindow` / `ScreenGeometry` / 面板定位，**没碰任何拖拽逻辑**。

- [x] ~~黑条常驻渲染~~ → 原来 DEBUG 半透明红、Release 全透明，现在是真黑条：
      `#000` + 下圆角 13pt，虚拟黑条上画「栖架」+ 条目数徽标（AccentColor 胶囊）
- [x] ~~刘海探测按真机数据落地~~ → 用 `auxiliaryTopLeftArea/RightArea` 反推刘海宽度。
      **本机实测：内置屏刘海 185×32，外接屏无刘海走虚拟黑条 186×30。**
      黑条尺寸取 `max(刘海, 186×30)` —— 比刘海小的话黑条整个藏进硬件里看不见，
      更糟的是面板会顶到刘海下沿以上，中间被摄像头模组咬掉一口
- [x] ~~横向形变到 640pt 与面板连成一体~~ → `cubic-bezier(.32,.72,0,1)` / 0.2s，
      用 `CADisplayLink` 逐帧改**窗口 frame**。圆角随进度 13 → 0（下面接着面板的 18pt 下圆角）
- [x] ~~1pt 抗锯齿缝~~ → 面板挂在黑条下沿并往上多压 1pt，那 1pt 被黑条盖住
- [x] ~~拖拽悬停时黑条高亮~~ → demo 那圈外发光做不到（窗口会裁掉超出 frame 的内容，
      要发光得让窗口比黑条大一圈，留给 M5），先用 2pt 内描边表达同一个意思
- [x] ~~尊重「减弱动态效果」~~ → 开了就直接到位，不做形变
- [x] ~~**验收（真机看一眼）**~~ → **2026-08-15 四条全部通过**：
      ① 外接屏顶部中央有黑条，「栖架」文字可见
      ② 悬停 → 黑条横向拉开成 640pt 并与面板连成一体，中间**没有亮缝**
      ③ 内置屏（有刘海）上从刘海两侧长出黑翅膀，面板顶部没有被刘海咬掉一口
      ④ 形变过程不掉帧，收起时反向播放

> ✅ **已定（2026-08-15）：选 A** —— 有刘海的屏幕上不画「栖架」和条目数徽标，
> 那块地方被摄像头模组挡着，画了也看不见；笔记本屏幕上的数量由面板头部显示。
> 「把徽标挪到左翅膀」属于新设计，不做。

### M2 · 剪贴板监听 + 文本列表 + 单击复制（约 3 天）

代码已全部写完，**0 错误 0 警告**。下面打勾的是已实现，最后一条验收要真机跑。

- [x] ~~`ClipboardWatcher`：0.5s 轮询 `changeCount`，按 图片 → 文件URL → 链接 → 文本 判定~~ →
      **命令行实测通过**：`osascript` 往剪贴板塞一张 PNG，1.5 秒内
      `blobs/<uuid>/content.png`（28884 B，与源文件一致）和 `thumb.jpg`（128×64，2111 B）都已落盘。
      两个坑写进注释了：Timer 必须加进 `.common` 模式（默认模式在菜单/拖拽期间整个停摆，
      表现是「按住鼠标时复制不上架」）；暂停期间也要吃掉 `changeCount`，
      否则一恢复就把暂停期间复制的东西倒灌进来。
      落地一律走 `DropIngestor` 的三个入口，不另写一份
- [x] ~~文本列表 UI（**不是卡片**），⌘1–9 序号，图片显示缩略图，链接绿色~~ →
      整行结构按主文档 2.3 补齐。剪贴板图片的缩略图走 ImageIO 生成
      （`QLThumbnailGenerator` 要源文件，手上只有 Data），列表里绝不拿原图当缩略图
- [x] ~~四条取出路径全部实现~~ → 单击复制 / ⌘1–9 / 拖出 / 双击固定。见下方 ⌘1–9 的口径改动
- [x] ~~复制反馈只做行内，**不弹全局 toast**~~ → 整行变绿描边 + 行内「✓ 已复制」，1.1 秒
- [x] ~~🔴 **待定：单击复制后要不要自动收起面板**~~ → **按原建议实现**：
      单击 → 行内反馈照放 → 1.1 秒后连同反馈一起收起；双击落在这 1.1 秒内则改走固定并撤销收起。
      收起那一步**故意越过了「没悬停就收起」这条判据**（鼠标就压在刚点的那行上，
      按判据算永远不会收）—— 这是全项目唯一一处越过它的地方，已在 `PanelController` 里注明
- [x] ~~面板头部常驻 ⏸ 暂停按钮，状态持久化~~ → 新增 `PanelHeaderView`
      （`栖架 剪贴板 N · 文件 N · 占用   ⏸ ⚙`），暂停状态存 UserDefaults。
      面板里的按钮**不能用 SwiftUI `Button`** —— 面板永远不是前台窗口，
      每次点击都是「非活跃窗口的第一次点击」，得走 AppKit 的 `acceptsFirstMouse`（`.clickAction`）
- [x] ~~**验收（必须真机）**~~ → **2026-08-15 五条全部通过**：
      ① 复制一段文本 → 自动上架 → 单击那一行 → 到备忘录 ⌘V 出来
      ② 复制截图 → 列表里显示缩略图 → 拖到聊天窗口对方收到 png
      ③ 面板展开时按 ⌘1 → 取回第一条；收起后按 ⌘1 → 还给浏览器切标签页
      ④ 双击某行 → 变成固定（★），面板不收起；单击别的行 → 1.1 秒后连人带面板一起收走
      ⑤ ⏸ 暂停后复制东西不上架，重启 App 后仍是暂停态
- [x] ~~全局唤出键 `⌃⌘V`~~ → 常驻注册，见下方说明

> ✅ **M2 完成。** 本产品的立身之本（剪贴板 + 文件合体 + 后台自动监听）到此成立。

> 🔴 **⌘1–⌘9 的口径和主文档不一样，改动是有意的。**
> 主文档写「全局快捷键，面板收起时也生效」，**没有照做** ——
> ⌘1–⌘9 在浏览器是切标签页、在访达是切视图，常驻注册等于把这九个键从所有 App 手里抢走。
> 现在：**面板展开时这九个键归栖架，收起时立刻还回去**。冲突窗口只存在于用户正盯着面板的那几秒。
> 用 Carbon `RegisterEventHotKey` 实现 —— 不需要辅助功能授权，而且能真正吃掉按键；
> `addGlobalMonitorForEvents` 两条都不满足（要授权，而且只能旁观、吞不掉，
> 会变成「浏览器切了标签页 + 栖架也复制了一条」同时发生）。

> ✅ **已定（2026-08-15）：全局唤出键改成 `⌃⌘V`，`⌘⇧V` 永不采用。**
> ⌘⇧V 是一大批 App 的「粘贴并匹配样式」，常驻注册会把它从所有 App 手里抢走。
> ⌃⌘V 上没有系统绑定，V 的记忆点还在。**不引入 KeyboardShortcuts 依赖** ——
> 同一个 `HotKeyCenter` 多注册一个键就够了（README 的致谢和快捷键表已同步改掉）。
> 键盘流程闭合：⌃⌘V 唤出 → ⌘1–⌘9 取第 N 条 → 取完 1.1 秒自动收起；
> 不想取了再按一次 ⌃⌘V（面板不抢焦点，收不到 Esc）。
> 改默认键的设置项在 M3。

### M3 · 持久化 + 自动清理 + 去重（约 2 天）
- [ ] `index.json` 原子写（写 `.tmp` 再 rename）；**图片/文件只存路径，绝不 base64 进 JSON**
- [ ] 去重只和当前区域最新一条比，命中重置 `createdAt`
- [ ] 保存时长设置项，默认 12 小时；启动一次 + 每 60 秒扫描；固定项永不过期
- [ ] 清理条目时同步删除 `blobs/` 目录，不留孤儿
- [ ] 剪贴板上限 200 条
- [ ] **验收**：退出重开内容还在；时长设成 1 小时验证清理生效且固定项不被清；连续复制同一文本不新增只重置

### M4 · 文件网格 + 多选 + 打包（约 4 天）
- [ ] 网格布局、多选（⌘/Shift/⌘A/Esc）、批量操作条
- [ ] 打包 ZIP（`NSFileCoordinator` `.forUploading`）
- [ ] 双击打开、右键菜单、AirDrop 分享
- [ ] **验收**：拖入 6 个不同类型文件网格不挤压；多选 3 个打包成 zip 拖到桌面能正常解压

### M5 · 热区适配 + 多屏 + 打磨（约 1 周）
- [x] ~~`ScreenGeometry`：刘海探测，无刘海机器渲染虚拟黑条，两种情况面板外观完全一致~~ → M1-c 已做
- [x] ~~多屏跟随鼠标；监听 `didChangeScreenParametersNotification` 重建窗口~~ → M1-b 已做
- [ ] Quick Look、深色/浅色、spring 动画、尊重 reduce motion
- [ ] 🎨 **面板外观对齐 `perch-demo-v3.html`** ——
      提案 2026-08-15 已确认采纳：<https://claude.ai/code/artifact/bd6150f5-78b9-49cf-8750-3ccbb615ef75>
      ~~第一步（黑条常驻 + 形变展开）~~ → **M1-c 已提前做完**（含那两个坑：1pt 抗锯齿缝、
      宽度必须真改窗口 frame）。**剩下的是面板本体**：现在还是 M1 的临时 UI（毛玻璃 + 裸列表），
      demo v3 那版才是定稿 —— 深色底 `rgba(24,24,27,.9)` + `blur(40px) saturate(180%)`、
      类型色标（文本橙 / 链接绿 / 图片紫）、行内操作按钮。
      还欠一个：拖拽悬停时黑条的**外发光**（要让窗口比黑条大一圈才画得出来，现在是内描边）。
      **形变那一下是「高级感」的主要来源**，不是模糊和圆角。
- [ ] i18n 收尾：全部字符串进 `.xcstrings`，补齐 en，**扫一遍确认没有硬编码中文**
- [ ] **验收**：外接屏上唤出正常；空格键预览；切英文界面无漏译；**冷启动到菜单栏图标 < 300ms**
- [ ] ⚡ 设计 App 图标（`Assets.xcassets`，1024×1024）

### M6 · 发布准备（约 3–5 天）
- [ ] 🔴 **生成 Sparkle EdDSA 密钥对，当天完成异地两份备份。** 用 `./bin/generate_keys` 生成，**私钥存进 macOS 登录钥匙串**（条目名 `Private key for signing Sparkle updates`），公钥填进 `Info.plist` 的 `SUPublicEDKey`。
      导出备份：`./bin/generate_keys -x <文件>`。查看：钥匙串访问.app 搜 `Sparkle`，或
      `security find-generic-password -s "https://sparkle-project.org" -a ed25519 -w`。
      **丢了就是死锁** —— 换新密钥要靠更新下发新公钥，而下发更新需要旧私钥签名。完整说明见签名手册第三章。
- [ ] Developer ID 签名 + notarize + DMG 打包脚本 `script/build_release.sh`
- [ ] 公证凭证：App-Specific Password 或 App Store Connect API Key，走 GitHub Secrets
- [ ] GitHub Actions：push tag → 构建 → 签名 → 公证 → DMG → 发 Release
- [ ] `appcast.xml` 生成脚本
- [ ] 录制演示 GIF → `docs/assets/demo.gif`（15 秒 / 10 MB 以内，README 两处占位在等它）
- [ ] `CONTRIBUTING.md`、`SECURITY.md`、`CODE_OF_CONDUCT.md`、Issue 模板（Bug / Feature）
- [ ] **验收**：在一台**没装过 Xcode 的干净 Mac** 上下载 DMG 双击能用，不报「已损坏」

---

## 阶段 3 · 自用两周

- [ ] 每天真实使用，记录所有卡顿和误操作
- [ ] 无崩溃满 14 天
- [ ] 复核[发布清单](docs/release-checklist.md)第六章的五条产品决定没有被「顺手优化」回去

> 这类工具的价值全在手感上。功能列表再长，唤出慢半拍就会被卸载。**这两周不要加新功能，只修手感。**

---

## 阶段 4 · 正式发布

- [ ] DMG 上传 COS `dl/Perch-1.0.0.dmg`，`appcast.xml` 上传 COS 根目录
- [ ] 记录 DMG 的 SHA-256 到腾讯云配置文档
- [ ] 无痕窗口点官网下载按钮，确认真的开始下载
- [ ] 官网下载区从「即将发布」换回真实链接，版本号与体积改成实际值
- [ ] 建 Homebrew tap 仓库 `KapiYue/homebrew-tap`，写 cask，实测 `brew install --cask perch`
- [ ] 走完[发布清单](docs/release-checklist.md)第二部分全部检查项
- [ ] GitHub Release 发布
- [ ] 发布后 24 小时内在 App 里实测一次 Sparkle 能检查到更新

---

## 阶段 5 · 发布之后再决定

这些**现在不要做**，做早了会污染判断。

- [ ] 变现方式（本期不做赞赏；一旦要收费，收款必须走站外平台，本站自建支付会触碰个人 ICP 备案的经营性红线）
- [ ] 是否换 GPL-3.0（想堵住被套壳卖钱就要换，且**贡献者进来之前换成本最低**）
- [ ] COS 自定义域名 `dl.perch.joy-coder.com`（默认域名首版够用）
- [ ] 是否新增 `/pricing`

---

## 关键路径

```
阶段 0（1 小时）
   ├─→ 阶段 1 官网上线 ────────────────────┐
   └─→ 阶段 2  M0 → M1 → M2 → M3 → M4 → M5 → M6 → 阶段 3 自用两周 ─→ 阶段 4 发布
                    ↑                                                    ↑
              生死线，卡住就停              官网下载链接在这里才换回真实地址
```

**总工期估算**：M0–M6 约 3.5 周纯开发，加自用两周，加官网上线（并行不占用），**到正式发布约 6 周**。

**唯一的高风险点是 M1 的 `NSFilePromiseProvider`。** 那一步通了，剩下的都是工作量问题。
